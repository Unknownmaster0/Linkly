version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: urlshortener
    ports:
      - "5432:5432"

  valkey:
    image: valkey/valkey:7
    ports:
      - "6379:6379"