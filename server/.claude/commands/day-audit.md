---
description: Run the end-of-day standards audit — compares the day's changes against the locked design docs and logs deviations to docs/dev-todos/todos.md
---

Delegate to the `day-end-audit` agent. Audit the codebase changed during the current
implementation day against the documented standards and update `docs/dev-todos/todos.md`
with any new deviations, marking previously-tracked items fixed where they now comply.

$ARGUMENTS
