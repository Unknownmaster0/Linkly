{
  "name": "url-shortener-backend",
  "private": true,
  "workspaces": [
    "api",
    "redirect",
    "shared",
    "worker"
  ],
  "scripts": {
    "build:api": "npm run build --prefix ./api",
    "build:redirect": "npm run build --prefix ./redirect",
    "build:shared": "npm run build --prefix ./shared",
    "build:worker": "npm run build --prefix ./worker",
    "dev:api": "npm run dev --prefix ./api",
    "dev:redirect": "npm run dev --prefix ./redirect",
    "dev:worker": "npm run dev --prefix ./worker"
  },
  "comments": {
    "overrides": "Force a single ioredis copy across the tree. bullmq pins ioredis to exact 5.10.1 while api/redirect use ^5.11.1; without this, two copies are installed and their types are nominally incompatible (BullMQ ConnectionOptions). 5.11.1 is API-compatible for bullmq."
  },
  "overrides": {
    "ioredis": "5.11.1"
  }
}
